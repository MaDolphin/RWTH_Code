/* generated by template od.DiagramVisitor*/




package appointmentsflexibilized._od;

import appointmentsflexibilized._visitor.AppointmentsFlexibilizedVisitor;
import appointmentsflexibilized._ast.ASTAppointmentsFlexibilizedNode;
import de.monticore.prettyprint.IndentPrinter;
import de.se_rwth.commons.Names;
import de.se_rwth.commons.StringTransformations;
import de.monticore.generating.templateengine.reporting.commons.ReportingRepository;
import java.util.Iterator;

public class AppointmentsFlexibilized2OD implements AppointmentsFlexibilizedVisitor {

  private AppointmentsFlexibilizedVisitor realThis = this;

  protected IndentPrinter pp;

  protected ReportingRepository reporting;

  protected boolean printEmptyOptional = false;

  protected boolean printEmptyList = false;

  public AppointmentsFlexibilized2OD(IndentPrinter printer, ReportingRepository reporting) {
    this.reporting = reporting;
    this.pp = printer;
  }


      @Override
      public void handle(appointmentsflexibilized._ast.ASTAppointment node) {
        String name = StringTransformations.uncapitalize(reporting.getASTNodeNameFormatted(node));
        printObject(name, "appointmentsflexibilized._ast.ASTAppointment");
        pp.indent();
          String scopeName = StringTransformations.uncapitalize(reporting.getScopeNameFormatted(node.getEnclosingScope()));
          pp.println("enclosingScope = " + scopeName + ";");
        printAttribute("string", "\"" + String.valueOf(node.getString()) + "\"");
        {
        Iterator<appointmentsflexibilized._ast.ASTI> iter_i = node.getIList().iterator();
        boolean isEmpty = true;
        if (iter_i.hasNext()) {
       	  pp.print("i");
   			  pp.print(" = [");
   			  pp.println("// *size: " + node.getIList().size());
				  pp.indent();
   			  isEmpty = false;
        } else if (printEmptyList) {
          pp.print("i");
          pp.println(" = [];");
        }
          boolean isFirst = true;
          while (iter_i.hasNext()) {
            if (!isFirst) {
              pp.println(",");
            }
            isFirst = false;
            iter_i.next().accept(getRealThis());
          }
          if (!isEmpty) {
            pp.println("];");
            pp.unindent();
          }
        }
        pp.unindent();
        pp.print("}");
     }


      @Override
      public void handle(appointmentsflexibilized._ast.ASTStart node) {
        String name = StringTransformations.uncapitalize(reporting.getASTNodeNameFormatted(node));
        printObject(name, "appointmentsflexibilized._ast.ASTStart");
        pp.indent();
          String scopeName = StringTransformations.uncapitalize(reporting.getScopeNameFormatted(node.getEnclosingScope()));
          pp.println("enclosingScope = " + scopeName + ";");
        if (node.isPresentDate()) {
     		  pp.print("date");
   			  pp.print(" = ");
          node.getDate().accept(getRealThis());
          pp.println(";");
        } else if (printEmptyOptional) {
          pp.println("date = absent;");
        }
        if (null != node.getTime()) {
      		pp.print("time");
          pp.print(" = ");
          node.getTime().accept(getRealThis());
          pp.println(";");
        }
        pp.unindent();
        pp.print("}");
     }


      @Override
      public void handle(appointmentsflexibilized._ast.ASTEnd node) {
        String name = StringTransformations.uncapitalize(reporting.getASTNodeNameFormatted(node));
        printObject(name, "appointmentsflexibilized._ast.ASTEnd");
        pp.indent();
          String scopeName = StringTransformations.uncapitalize(reporting.getScopeNameFormatted(node.getEnclosingScope()));
          pp.println("enclosingScope = " + scopeName + ";");
        if (node.isPresentDate()) {
     		  pp.print("date");
   			  pp.print(" = ");
          node.getDate().accept(getRealThis());
          pp.println(";");
        } else if (printEmptyOptional) {
          pp.println("date = absent;");
        }
        if (null != node.getTime()) {
      		pp.print("time");
          pp.print(" = ");
          node.getTime().accept(getRealThis());
          pp.println(";");
        }
        pp.unindent();
        pp.print("}");
     }


      @Override
      public void handle(appointmentsflexibilized._ast.ASTDate node) {
        String name = StringTransformations.uncapitalize(reporting.getASTNodeNameFormatted(node));
        printObject(name, "appointmentsflexibilized._ast.ASTDate");
        pp.indent();
          String scopeName = StringTransformations.uncapitalize(reporting.getScopeNameFormatted(node.getEnclosingScope()));
          pp.println("enclosingScope = " + scopeName + ";");
        printAttribute("digits", String.valueOf(node.getDigitsList()));
        pp.unindent();
        pp.print("}");
     }


      @Override
      public void handle(appointmentsflexibilized._ast.ASTTime node) {
        String name = StringTransformations.uncapitalize(reporting.getASTNodeNameFormatted(node));
        printObject(name, "appointmentsflexibilized._ast.ASTTime");
        pp.indent();
          String scopeName = StringTransformations.uncapitalize(reporting.getScopeNameFormatted(node.getEnclosingScope()));
          pp.println("enclosingScope = " + scopeName + ";");
        printAttribute("digits", String.valueOf(node.getDigitsList()));
        pp.unindent();
        pp.print("}");
     }


      @Override
      public void handle(appointmentsflexibilized._ast.ASTBreak node) {
        String name = StringTransformations.uncapitalize(reporting.getASTNodeNameFormatted(node));
        printObject(name, "appointmentsflexibilized._ast.ASTBreak");
        pp.indent();
          String scopeName = StringTransformations.uncapitalize(reporting.getScopeNameFormatted(node.getEnclosingScope()));
          pp.println("enclosingScope = " + scopeName + ";");
        if (null != node.getStart()) {
      		pp.print("start");
          pp.print(" = ");
          node.getStart().accept(getRealThis());
          pp.println(";");
        }
        if (null != node.getEnd()) {
      		pp.print("end");
          pp.print(" = ");
          node.getEnd().accept(getRealThis());
          pp.println(";");
        }
        pp.unindent();
        pp.print("}");
     }


      @Override
      public void handle(appointmentsflexibilized._ast.ASTParticipants node) {
        String name = StringTransformations.uncapitalize(reporting.getASTNodeNameFormatted(node));
        printObject(name, "appointmentsflexibilized._ast.ASTParticipants");
        pp.indent();
          String scopeName = StringTransformations.uncapitalize(reporting.getScopeNameFormatted(node.getEnclosingScope()));
          pp.println("enclosingScope = " + scopeName + ";");
        printAttribute("string", String.valueOf(node.getStringList()));
        pp.unindent();
        pp.print("}");
     }


      @Override
      public void handle(appointmentsflexibilized._ast.ASTFrequency node) {
        String name = StringTransformations.uncapitalize(reporting.getASTNodeNameFormatted(node));
        printObject(name, "appointmentsflexibilized._ast.ASTFrequency");
        pp.indent();
          String scopeName = StringTransformations.uncapitalize(reporting.getScopeNameFormatted(node.getEnclosingScope()));
          pp.println("enclosingScope = " + scopeName + ";");
        pp.unindent();
        pp.print("}");
     }


  private void printAttribute(String name, String value) {
    pp.print(name);
    pp.print(" = ");
    pp.print(value);
    pp.println(";");
  }

  private void printObject(String objName, String objType) {
    pp.print(objName);
    pp.print(":");
    pp.print(Names.getSimpleName(objType));
    pp.println(" {");
  }

  public String printObjectDiagram(String modelName, ASTAppointmentsFlexibilizedNode node) {
    pp.clearBuffer();
    pp.setIndentLength(2);
    pp.print("objectdiagram ");
    pp.print(modelName);
    pp.println(" {");
    pp.indent();
    node.accept(getRealThis());
    pp.print(";");
    pp.unindent();
    pp.println("}");
    return pp.getContent();
  }

  @Override
  public void setRealThis(AppointmentsFlexibilizedVisitor realThis) {
    this.realThis = realThis;
  }

  @Override
  public AppointmentsFlexibilizedVisitor getRealThis() {
    return realThis;
  }

  /**
   * @return the printEmptyOptional
   */
  public boolean isPrintEmptyOptional() {
    return this.printEmptyOptional;
  }


  /**
   * @param printEmptyOptional the printEmptyOptional to set
   */
  public void setPrintEmptyOptional(boolean printEmptyOptional) {
    this.printEmptyOptional = printEmptyOptional;
  }


  /**
   * @return the printEmptyList
   */
  public boolean isPrintEmptyList() {
    return this.printEmptyList;
  }


  /**
   * @param printEmptyList the printEmptyList to set
   */
  public void setPrintEmptyList(boolean printEmptyList) {
    this.printEmptyList = printEmptyList;
  }

}

