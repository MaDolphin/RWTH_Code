/* generated by template od.DiagramVisitor*/




package appointments._od;

import appointments._visitor.AppointmentsVisitor;
import appointments._ast.ASTAppointmentsNode;
import de.monticore.prettyprint.IndentPrinter;
import de.se_rwth.commons.Names;
import de.se_rwth.commons.StringTransformations;
import de.monticore.generating.templateengine.reporting.commons.ReportingRepository;
import java.util.Iterator;

public class Appointments2OD implements AppointmentsVisitor {

  private AppointmentsVisitor realThis = this;

  protected IndentPrinter pp;

  protected ReportingRepository reporting;

  protected boolean printEmptyOptional = false;

  protected boolean printEmptyList = false;

  public Appointments2OD(IndentPrinter printer, ReportingRepository reporting) {
    this.reporting = reporting;
    this.pp = printer;
  }


      @Override
      public void handle(appointments._ast.ASTAppointment node) {
        String name = StringTransformations.uncapitalize(reporting.getASTNodeNameFormatted(node));
        printObject(name, "appointments._ast.ASTAppointment");
        pp.indent();
          String scopeName = StringTransformations.uncapitalize(reporting.getScopeNameFormatted(node.getEnclosingScope()));
          pp.println("enclosingScope = " + scopeName + ";");
        if (null != node.getStart()) {
      		pp.print("start");
          pp.print(" = ");
          node.getStart().accept(getRealThis());
          pp.println(";");
        }
        if (null != node.getEnd()) {
      		pp.print("end");
          pp.print(" = ");
          node.getEnd().accept(getRealThis());
          pp.println(";");
        }
        printAttribute("string", String.valueOf(node.getStringList()));
        {
        Iterator<appointments._ast.ASTBreak> iter_r__break = node.getBreakList().iterator();
        boolean isEmpty = true;
        if (iter_r__break.hasNext()) {
       	  pp.print("r__break");
   			  pp.print(" = [");
   			  pp.println("// *size: " + node.getBreakList().size());
				  pp.indent();
   			  isEmpty = false;
        } else if (printEmptyList) {
          pp.print("r__break");
          pp.println(" = [];");
        }
          boolean isFirst = true;
          while (iter_r__break.hasNext()) {
            if (!isFirst) {
              pp.println(",");
            }
            isFirst = false;
            iter_r__break.next().accept(getRealThis());
          }
          if (!isEmpty) {
            pp.println("];");
            pp.unindent();
          }
        }
        pp.unindent();
        pp.print("}");
     }


      @Override
      public void handle(appointments._ast.ASTStart node) {
        String name = StringTransformations.uncapitalize(reporting.getASTNodeNameFormatted(node));
        printObject(name, "appointments._ast.ASTStart");
        pp.indent();
          String scopeName = StringTransformations.uncapitalize(reporting.getScopeNameFormatted(node.getEnclosingScope()));
          pp.println("enclosingScope = " + scopeName + ";");
        if (node.isPresentDate()) {
     		  pp.print("date");
   			  pp.print(" = ");
          node.getDate().accept(getRealThis());
          pp.println(";");
        } else if (printEmptyOptional) {
          pp.println("date = absent;");
        }
        if (null != node.getTime()) {
      		pp.print("time");
          pp.print(" = ");
          node.getTime().accept(getRealThis());
          pp.println(";");
        }
        pp.unindent();
        pp.print("}");
     }


      @Override
      public void handle(appointments._ast.ASTEnd node) {
        String name = StringTransformations.uncapitalize(reporting.getASTNodeNameFormatted(node));
        printObject(name, "appointments._ast.ASTEnd");
        pp.indent();
          String scopeName = StringTransformations.uncapitalize(reporting.getScopeNameFormatted(node.getEnclosingScope()));
          pp.println("enclosingScope = " + scopeName + ";");
        if (node.isPresentDate()) {
     		  pp.print("date");
   			  pp.print(" = ");
          node.getDate().accept(getRealThis());
          pp.println(";");
        } else if (printEmptyOptional) {
          pp.println("date = absent;");
        }
        if (null != node.getTime()) {
      		pp.print("time");
          pp.print(" = ");
          node.getTime().accept(getRealThis());
          pp.println(";");
        }
        pp.unindent();
        pp.print("}");
     }


      @Override
      public void handle(appointments._ast.ASTDate node) {
        String name = StringTransformations.uncapitalize(reporting.getASTNodeNameFormatted(node));
        printObject(name, "appointments._ast.ASTDate");
        pp.indent();
          String scopeName = StringTransformations.uncapitalize(reporting.getScopeNameFormatted(node.getEnclosingScope()));
          pp.println("enclosingScope = " + scopeName + ";");
        printAttribute("digits", String.valueOf(node.getDigitsList()));
        pp.unindent();
        pp.print("}");
     }


      @Override
      public void handle(appointments._ast.ASTTime node) {
        String name = StringTransformations.uncapitalize(reporting.getASTNodeNameFormatted(node));
        printObject(name, "appointments._ast.ASTTime");
        pp.indent();
          String scopeName = StringTransformations.uncapitalize(reporting.getScopeNameFormatted(node.getEnclosingScope()));
          pp.println("enclosingScope = " + scopeName + ";");
        printAttribute("digits", String.valueOf(node.getDigitsList()));
        pp.unindent();
        pp.print("}");
     }


      @Override
      public void handle(appointments._ast.ASTBreak node) {
        String name = StringTransformations.uncapitalize(reporting.getASTNodeNameFormatted(node));
        printObject(name, "appointments._ast.ASTBreak");
        pp.indent();
          String scopeName = StringTransformations.uncapitalize(reporting.getScopeNameFormatted(node.getEnclosingScope()));
          pp.println("enclosingScope = " + scopeName + ";");
        if (null != node.getStart()) {
      		pp.print("start");
          pp.print(" = ");
          node.getStart().accept(getRealThis());
          pp.println(";");
        }
        if (null != node.getEnd()) {
      		pp.print("end");
          pp.print(" = ");
          node.getEnd().accept(getRealThis());
          pp.println(";");
        }
        pp.unindent();
        pp.print("}");
     }


  private void printAttribute(String name, String value) {
    pp.print(name);
    pp.print(" = ");
    pp.print(value);
    pp.println(";");
  }

  private void printObject(String objName, String objType) {
    pp.print(objName);
    pp.print(":");
    pp.print(Names.getSimpleName(objType));
    pp.println(" {");
  }

  public String printObjectDiagram(String modelName, ASTAppointmentsNode node) {
    pp.clearBuffer();
    pp.setIndentLength(2);
    pp.print("objectdiagram ");
    pp.print(modelName);
    pp.println(" {");
    pp.indent();
    node.accept(getRealThis());
    pp.print(";");
    pp.unindent();
    pp.println("}");
    return pp.getContent();
  }

  @Override
  public void setRealThis(AppointmentsVisitor realThis) {
    this.realThis = realThis;
  }

  @Override
  public AppointmentsVisitor getRealThis() {
    return realThis;
  }

  /**
   * @return the printEmptyOptional
   */
  public boolean isPrintEmptyOptional() {
    return this.printEmptyOptional;
  }


  /**
   * @param printEmptyOptional the printEmptyOptional to set
   */
  public void setPrintEmptyOptional(boolean printEmptyOptional) {
    this.printEmptyOptional = printEmptyOptional;
  }


  /**
   * @return the printEmptyList
   */
  public boolean isPrintEmptyList() {
    return this.printEmptyList;
  }


  /**
   * @param printEmptyList the printEmptyList to set
   */
  public void setPrintEmptyList(boolean printEmptyList) {
    this.printEmptyList = printEmptyList;
  }

}

